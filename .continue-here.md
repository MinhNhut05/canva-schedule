---
phase: adhoc-programName-field-split
task: 3
total_tasks: 3
status: code_complete
last_updated: 2026-03-31T03:07:09.448Z
---

<current_state>
All code changes for splitting programName from title are done and unit tests pass (40/40).
The work is NOT committed yet. Next step is verifying Canva template binding + manual E2E test.
</current_state>

<completed_work>

- Task 1: Add programName to extraction schema + AI prompt - Done
  - `src/lib/ai/extraction-schema.ts` — added `programName: z.string().nullish()` to commonFieldsSchema
  - `src/lib/ai/extraction-prompt.ts` — split semantics: programName = heading, title = route/school; added rule #9
- Task 2: Update review UI and Canva payload mapping - Done
  - `src/components/review/itinerary-editor.tsx` — added "Ten chuong trinh" editable field above title; relabeled title to "Tieu de tuyen / truong"
  - `src/lib/canva/payload.ts` — added `programName` to DraftBase; `program_label` now uses `resolveProgramLabel()` with fallback to "CHƯƠNG TRÌNH THAM QUAN"; `title` stays route/school text
- Task 3: Update tests - Done
  - `src/lib/ai/__tests__/extraction-schema.test.ts` — updated fixtures with programName, added assertions
  - `src/lib/ai/__tests__/extract-tour.test.ts` — updated mock response, added programName assertions
  - `src/lib/canva/__tests__/payload.test.ts` — updated shared draft, added fallback test case
</completed_work>

<remaining_work>

- Verify Canva template binding: confirm the top heading text element is bound to `program_label` field, not static text
- Manual E2E test: upload a DOCX/PDF, check review page shows programName + title separately, generate Canva output
- Commit all changes
</remaining_work>

<decisions_made>

- programName is auto-extracted from document with manual override in review UI
- Fallback to "CHƯƠNG TRÌNH THAM QUAN" happens only at Canva payload build time (in `resolveProgramLabel()`)
- Stored draft keeps programName blank/null when extraction is uncertain — no synthetic values
- `program_label` Canva field gets programName; `title` Canva field keeps route/school text
- No Prisma migration needed — programName lives inside structuredDraft JSON
</decisions_made>

<blockers>
- Canva template may have static heading text "CHƯƠNG TRÌNH THAM QUAN" instead of data-bound `program_label`. If so, the template design needs manual update in Canva.
</blockers>

<context>
User wanted each uploaded DOCX/PDF to have its own program heading (e.g. "CHƯƠNG TRÌNH HƯỚNG NGHIỆP TÌM HIỂU NGÀNH NGHỀ") instead of the generic "CHƯƠNG TRÌNH THAM QUAN". The title field should remain the school/route text (e.g. "SÓC TRĂNG – CẦN THƠ"). field-map.ts already had both `title` and `program_label` keys but they were both sourced from `draft.title`. Now they carry distinct semantics.
</context>

<next_action>
Start with: Check if the Canva template binds its top heading element to `program_label`. If static, update the template. Then do a full manual test: upload → review → generate.
</next_action>
