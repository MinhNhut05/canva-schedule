---
phase: 04-editable-canva-generation
task: null
total_tasks: null
status: debug_session_complete
last_updated: "2026-04-01T04:23:23.474Z"
---

<current_state>
Debug session for Canva autofill failure on TWO_DAY templates.
API probe confirmed both ITINERARY and MENU autofill now succeed.
Awaiting user UI test to fully close this issue.
</current_state>

<completed_work>

- Diagnosed `design_not_fillable` error for TWO_DAY (Lịch trình + Thực đơn)
- Found DB template IDs had been changed to wrong values → reverted to .env values
- Discovered .env template IDs also failed → probed Canva API directly
- Root cause: TWO_DAY Brand Templates on Canva had **empty dataset** (`{}`) — Bulk Create fields were configured in UI but **not published**
- Guided user to **Publish brand template** on Canva
- Verified via API: both templates now return correct dataset fields
  - `EAHFavTKu_k` (2DAY ITINERARY): `title`, `program_label`, `tour_date`, `day1_block`, `day2_block`
  - `EAHFaunQ-m8` (2DAY MENU): `title`, `program_label`, `tour_date`, `menu_day1_block`, `menu_day2_block`
- Ran full autofill probe with real upload data (`cmnfd58r000074yj047up8z9e`) — both succeeded with edit URLs
</completed_work>

<remaining_work>

- User UI test: hit "Thử lại" on TWO_DAY upload to confirm app-level generation works
- Optionally: verify generated Canva designs have correct content placement/formatting
</remaining_work>

<decisions_made>

- Root cause was **Canva-side configuration**, not code — Brand Templates lacked published autofill dataset fields
- DB template IDs were reverted from `EAHFhGCm2I8`/`EAHFhGoflFk` to `EAHFavTKu_k`/`EAHFaunQ-m8` (matching .env)
- Key learning: Canva requires **Publish brand template** after adding Bulk Create fields — just saving/Done is not enough
</decisions_made>

<blockers>
None — issue is resolved pending UI confirmation.
</blockers>

<context>
This was a pure debug session. Phase 04 plans (04-01 through 04-03) were already completed previously.
The Canva autofill failure was a configuration issue, not a code bug.
All 4 templates (ONE_DAY + TWO_DAY × ITINERARY + MENU) are now confirmed working via API probe.
The fieldMapping in DB uses identity mapping (canonical key → same key) which matches Canva dataset field names.
</context>

<next_action>
Start with: User tests TWO_DAY Canva generation on the app UI.
If success → debug session complete, no code changes needed.
If failure → check error message, likely token refresh or different issue.
</next_action>
