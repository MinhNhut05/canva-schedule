---
phase: 04-editable-canva-generation
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-03-31T08:41:23.384Z
---

<current_state>
We are resuming follow-up work inside Phase 04 for the user's new request: support/finish **menu 1 ngày**, **menu 2 ngày**, and **lịch trình 2 ngày** output behavior so generated Canva results match the provided sample images for the 2-day itinerary and 2-day menu.

This session stopped before code changes because conversation context was exhausted. The work completed here was planning-only: scope clarification, codebase inspection, and an approved implementation direction.
</current_state>

<completed_work>

- Task 1: Clarified user intent and constraints - Done
  - User wants work on menu 1 ngày, menu 2 ngày, and lịch trình 2 ngày.
  - Reuse the existing 1-day mapping/data flow.
  - Ask questions before doing work; user requested proposal first.
  - Desired visual target is the two provided sample images for 2-day itinerary + 2-day menu.

- Task 2: Inspected current repo state for Canva generation - Done
  - Current main branch already supports explicit `ONE_DAY` and `TWO_DAY` branches in:
    - `src/lib/canva/payload.ts`
    - `src/lib/canva/field-map.ts`
    - `src/app/(app)/review/[id]/actions.ts`
    - review editors / template admin flow
  - Important discovery: current code uses **block-based fields**, not per-slot fields.
    - Examples: `morning_block`, `afternoon_block`, `day1_block`, `day2_block`, `menu_day1_block`, `menu_day2_block`.

- Task 3: Produced approved implementation direction - Done
  - Highest-priority fix is likely applying saved `fieldMapping` from CanvaTemplate records at runtime during generation.
  - Keep the current block-based payload model unless the sample layouts prove a truly missing field.
  - Add focused test coverage after runtime mapping is wired.
</completed_work>

<remaining_work>

- Task 1: Implement runtime fieldMapping remap
  - Start in `src/lib/canva/template-resolver.ts` and `src/lib/canva/adapter.ts`.
  - Goal: load the full active CanvaTemplate record and translate canonical payload keys into the actual Canva text-element names saved in `fieldMapping` before calling Canva APIs.

- Task 2: Add/update tests for the resume scope
  - Cover runtime mapping application.
  - Cover menu 1 ngày, menu 2 ngày, and lịch trình 2 ngày generation paths.
  - Likely files:
    - `src/app/(app)/review/[id]/__tests__/actions.test.ts`
    - `src/lib/canva/__tests__/payload.test.ts`
    - possibly admin template tests if mapping behavior changes

- Task 3: Verify templates against the sample-image contract
  - After code changes, confirm admin mappings / real Canva element names are correct.
  - Compare generated outputs for:
    - menu 1 ngày
    - menu 2 ngày
    - lịch trình 2 ngày
  - Specifically verify the 2-day outputs visually match the user's two reference images.
</remaining_work>

<decisions_made>

- Decided to keep the existing **block-text payload** approach because the current repo already implements it in `src/lib/canva/field-map.ts` and `src/lib/canva/payload.ts`.
- Decided not to redesign schema/editor flow yet because explicit ONE_DAY/TWO_DAY support already exists and appears functionally sufficient for the requested scope.
- Chose to prioritize runtime use of saved `fieldMapping` over payload redesign because admin already stores mappings, but generation appears to bypass them.
- Treated this work as a Phase 04 follow-up because it directly extends the editable Canva generation flow rather than Phase 05 admin/history or Phase 06 polish.
</decisions_made>

<blockers>
- Context exhaustion: implementation was paused intentionally before edits. Workaround: resume in a fresh session and start directly from the approved plan + this handoff.
- Visual correctness still depends on real Canva template element names and admin mappings after the runtime remap is implemented.
</blockers>

<context>
Important current understanding:
- The repo state is newer than some old planning docs. Trust current code over older assumptions.
- Current `src/lib/canva/field-map.ts` defines only these shared fields: `title`, `program_label`, `tour_date`.
- Current `src/lib/canva/payload.ts` already formats itinerary/menu content into joined block strings and appends `Kết thúc chương trình!` to the final itinerary block.
- `src/app/(app)/review/[id]/actions.ts` already branches correctly between `buildOneDay*` and `buildTwoDay*` payload builders.
- `src/app/(app)/admin/templates/_components/template-edit-sheet.tsx` already lets admin edit `fieldMapping`, but planning strongly suggests that stored mapping is not actually applied in the runtime generation path yet.
- The broader working tree already had many unrelated uncommitted changes before pause-work started. Do not assume a clean branch when resuming.
</context>

<next_action>
Start with: read `src/lib/canva/template-resolver.ts`, `src/lib/canva/adapter.ts`, and `src/app/(app)/review/[id]/actions.ts`, then implement the smallest safe change that loads the active CanvaTemplate record and remaps canonical payload keys through `fieldMapping` before sending data to Canva.
</next_action>
