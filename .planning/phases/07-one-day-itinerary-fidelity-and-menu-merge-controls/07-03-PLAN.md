---
plan: 07-03
phase: 7
type: implementation
title: "Apply one-day menu merge option in Canva payloads and regression coverage"
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/lib/canva/payload.ts
  - src/lib/canva/__tests__/payload.test.ts
  - src/app/(app)/review/[id]/actions.ts
  - src/components/review/review-page.tsx
  - tests/e2e/review-canva-generation.spec.ts
requirements:
  - CANVA-08
  - UX-06
autonomous: true
must_haves:
  truths:
    - "The 1-day itinerary Canva payload respects the saved per-upload menu-merge option"
    - "Merged meal/menu lines are inserted only into the relevant 1-day itinerary sections"
    - "Brand-critical fields like `program_label` and short `title` remain separate and map from reviewed data"
  artifacts:
    - src/lib/canva/payload.ts
    - src/lib/canva/__tests__/payload.test.ts
    - src/app/(app)/review/[id]/actions.ts
  key_links:
    - src/components/review/review-page.tsx
    - src/lib/review/draft.ts
---

# Plan 07-03: Apply One-day Menu Merge Option in Canva Payloads and Regression Coverage

## Objective

Finish the phase by making the saved 1-day review options affect the actual Canva data sent downstream. This plan wires the new option into payload generation, keeps the approved brand-label/title separation intact, and adds regression coverage for the canonical Day 1 sample.

## Tasks

<task id="3.1">
<name>Teach the 1-day payload builder to use reviewed title/program label and optional merged menu lines</name>
<read_first>
- src/lib/canva/payload.ts
- src/lib/canva/__tests__/payload.test.ts
- src/lib/ai/extraction-schema.ts
- .planning/phases/07-one-day-itinerary-fidelity-and-menu-merge-controls/07-CONTEXT.md
</read_first>
<files>
- src/lib/canva/payload.ts
- src/lib/canva/__tests__/payload.test.ts
</files>
<action>
Refactor the 1-day payload path so it:

- uses reviewed `programName` as the preferred `program_label` source when present
- keeps the reviewed short `title` separate from `program_label`
- conditionally injects concise meal/menu lines into the relevant itinerary block when `mergeMenuIntoItinerary` is enabled
- preserves the conservative wording created in Plan 07-01 instead of re-shortening during payload assembly

Payload helpers may add small internal utilities for line joining or section-aware insertion, but they must keep the existing adapter boundary intact.
</action>
<acceptance_criteria>
- `src/lib/canva/payload.ts` accepts the saved 1-day option when building the itinerary payload
- program label and title are still emitted as separate fields
- merged menu lines only affect 1-day itinerary payloads when enabled
- `src/lib/canva/__tests__/payload.test.ts` covers enabled and disabled merge behavior
</acceptance_criteria>
</task>

<task id="3.2">
<name>Wire generate/retry actions to the saved 1-day option and warning state</name>
<read_first>
- src/app/(app)/review/[id]/actions.ts
- src/components/review/review-page.tsx
- .planning/phases/07-one-day-itinerary-fidelity-and-menu-merge-controls/07-02-PLAN.md
</read_first>
<files>
- src/app/(app)/review/[id]/actions.ts
- src/components/review/review-page.tsx
</files>
<action>
Update generation actions so 1-day Canva generation reads the saved menu-merge option and any related warning state from persistence instead of from transient client-only state.

Regenerate and retry must stay consistent:
- saved option remains authoritative
- warning copy stays visible when relevant
- 2-day generation behavior remains unchanged
</action>
<acceptance_criteria>
- `generateCanva` and retry paths read the saved 1-day option from the durable source
- 1-day regenerate behavior is consistent across refreshes
- 2-day generation code path is unaffected
</acceptance_criteria>
</task>

<task id="3.3">
<name>Add canonical end-to-end regression for the Day 1 sample</name>
<read_first>
- tests/e2e/review-canva-generation.spec.ts
- /home/minhnhut_dev/Downloads/TH LONG TUYỀN 2 - BT CHỨNG TÍCH CHIẾN TRANH - DINH - BẾN NHÀ RỒNG - SUỐI TIÊN - METRO - 1803.pdf
- thread image #2
</read_first>
<files>
- tests/e2e/review-canva-generation.spec.ts
</files>
<action>
Extend regression coverage so the mocked 1-day Canva flow proves:

- `program_label` uses the canonical heading
- the 13:00 condensed block matches the approved wording shape more closely than the current over-expanded variant
- the 15:30 return line keeps the full destination
- enabling/disabling menu merge produces different itinerary payload output in a predictable way
</action>
<acceptance_criteria>
- e2e or integration coverage asserts canonical Phase 7 output characteristics for the 1-day sample
- payload-level assertions catch regression if the destination or program label collapses again
</acceptance_criteria>
</task>

## Verification

```bash
npx vitest run src/lib/canva/__tests__/payload.test.ts
npx playwright test tests/e2e/review-canva-generation.spec.ts
```

## Done When

The saved 1-day option changes the actual Canva output, the conservative wording survives end to end, and the canonical school sample is guarded by regression coverage.
