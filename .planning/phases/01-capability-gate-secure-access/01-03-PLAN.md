---
phase: 01-capability-gate-secure-access
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/canva-probe.ts
  - .planning/phases/01-capability-gate-secure-access/01-CANVA-PROBE-RESULT.md
autonomous: true
requirements:
  - CANVA-07
user_setup:
  - service: canva
    why: "Live verification of the real SOHA Travel autofill path before Phase 4 depends on it"
    env_vars:
      - name: CANVA_CLIENT_ID
        source: "Canva developer/integration settings for the real SOHA Travel Canva workspace"
      - name: CANVA_CLIENT_SECRET
        source: "Canva developer/integration settings for the real SOHA Travel Canva workspace"
      - name: CANVA_ACCESS_TOKEN
        source: "OAuth/token flow for the real SOHA Travel Canva workspace"
      - name: CANVA_REFRESH_TOKEN
        source: "OAuth/token flow for the real SOHA Travel Canva workspace"
      - name: CANVA_TEMPLATE_ID
        source: "The real SOHA Travel Brand Template ID used for production verification"
    dashboard_config:
      - task: "Ensure the target SOHA itinerary/menu template is published as a real Brand Template in the connected workspace"
        location: "Canva workspace / Brand Template management"
must_haves:
  truths:
    - "The Canva capability probe runs against real SOHA credentials and a real SOHA template, not placeholders or a synthetic demo template."
    - "The Phase 1 go/no-go result is based on all three required verification steps: API connectivity, template autofill, and editable link retrieval."
    - "If any verification step fails, the result document captures the blocker with exact failure details so downstream Canva work does not proceed blindly."
  artifacts:
    - path: "scripts/canva-probe.ts"
      provides: "Repeatable automated Canva feasibility probe"
      exports: ["main"]
    - path: ".planning/phases/01-capability-gate-secure-access/01-CANVA-PROBE-RESULT.md"
      provides: "GO/NO-GO decision record from the live probe"
      contains: "Verdict"
  key_links:
    - from: "scripts/canva-probe.ts"
      to: "src/lib/env.ts"
      via: "real Canva credential accessor"
      pattern: "getCanvaEnv"
    - from: "scripts/canva-probe.ts"
      to: "CANVA_TEMPLATE_ID"
      via: "required real SOHA Brand Template id"
      pattern: "CANVA_TEMPLATE_ID"
    - from: "01-CANVA-PROBE-RESULT.md"
      to: "scripts/canva-probe.ts"
      via: "documented output from actual run"
      pattern: "Step 1|Step 2|Step 3"
---

<objective>
Run the real Canva capability gate for Phase 1 and produce a concrete go/no-go record before downstream Canva generation work begins.

Purpose: Fulfill CANVA-07 exactly as discussed by proving or disproving the intended autofill workflow with the real SOHA Canva account and real SOHA template conditions.
Output: Automated probe script plus a live-result markdown record documenting either all three successful steps or a blocker with actionable evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/minhnhut_dev/projects/siletravel/.planning/PROJECT.md
@/home/minhnhut_dev/projects/siletravel/.planning/ROADMAP.md
@/home/minhnhut_dev/projects/siletravel/.planning/REQUIREMENTS.md
@/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-CONTEXT.md
@/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-RESEARCH.md

<interfaces>
Required contract from 01-01:

From `src/lib/env.ts`:
```ts
export function getCanvaEnv(): {
  CANVA_CLIENT_ID: string
  CANVA_CLIENT_SECRET: string
  CANVA_ACCESS_TOKEN: string
  CANVA_REFRESH_TOKEN: string
  CANVA_TEMPLATE_ID: string
}
```
</interfaces>

<decisions>
- D-09: probe must be automated and repeatable.
- D-10: all 3 verification steps must pass — connect to Canva API, fill template text fields, receive editable Canva link.
- D-11: any failure must be documented as a blocker; no fallback output strategy is implemented here.
- D-12: verification must use the real SOHA Travel Canva template, not a mock or generic sample.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a real-credential-only Canva probe script for the locked three-step gate</name>
  <files>scripts/canva-probe.ts</files>
  <action>Create `scripts/canva-probe.ts` as a repeatable Node/tsx script that loads env using `@next/env`, then reads Canva credentials via `getCanvaEnv()` from 01-01. The script must reject placeholder-looking values (for example `change-me`, `placeholder`, `your-canva-*`) and must require `CANVA_TEMPLATE_ID` explicitly so D-12 always targets the real SOHA template rather than auto-discovering any template. Implement exactly three gate steps per D-10: (1) connect to Canva successfully and confirm the specified template is reachable in the authenticated workspace, (2) fetch the real template dataset and submit an autofill job that writes probe text into available text fields, and (3) poll until completion and require an editable Canva URL in the result. Exit 0 only if all three pass. Exit non-zero on any failure and print structured machine-readable details including step name, HTTP status, response excerpt, template id, and final verdict. Do not allow dry-run success, fake tokens, or mock template substitutes.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>A repeatable probe script exists that can only produce GO from a real Canva account/template run and can only produce NO-GO with explicit failure evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run the live probe against the real SOHA template and document GO or blocker evidence</name>
  <files>.planning/phases/01-capability-gate-secure-access/01-CANVA-PROBE-RESULT.md</files>
  <action>Execute the probe against the real SOHA Travel Canva credentials and the real SOHA Brand Template ID from env. Capture the raw result and write `.planning/phases/01-capability-gate-secure-access/01-CANVA-PROBE-RESULT.md` with: run timestamp, workspace/template identifiers used, step-by-step PASS/FAIL results, exact HTTP status codes, dataset field summary, editable URL when available, and final `Verdict: GO` or `Verdict: NO-GO`. If any step fails, document the blocker per D-11 with the exact failing step, probable root cause, whether it points to plan limitations (for example Brand Templates/Enterprise requirements), and what must change before Phase 4 can rely on Canva. Do not mark the plan complete from placeholder credentials, skipped steps, or a syntax-only run; CANVA-07 is only satisfied by a real live verification attempt.</action>
  <verify>
    <automated>bash -lc 'test -n "$CANVA_ACCESS_TOKEN" && test "$CANVA_ACCESS_TOKEN" != "your-canva-access-token" && test "$CANVA_ACCESS_TOKEN" != "placeholder" && test -n "$CANVA_TEMPLATE_ID" && test "$CANVA_TEMPLATE_ID" != "your-brand-template-id" && npx tsx scripts/canva-probe.ts'</automated>
  </verify>
  <done>The phase has a trustworthy GO/NO-GO record based on a real live probe, and any failure is documented well enough to stop downstream Canva dependence or unblock it with concrete next steps.</done>
</task>

</tasks>

<verification>
- `scripts/canva-probe.ts` fails fast on placeholder credentials or missing `CANVA_TEMPLATE_ID`.
- The live run covers all 3 required steps, not just connectivity or a dry-run payload build.
- `01-CANVA-PROBE-RESULT.md` records either a GO with editable URL evidence or a NO-GO with blocker details and exact failure evidence.
</verification>

<success_criteria>
- CANVA-07 is verified using the real SOHA template path, not assumptions.
- The team has a concrete decision artifact for whether the planned Canva autofill workflow is viable.
- If the workflow is not viable, the blocker is documented before Phase 4 starts depending on it.
</success_criteria>

<output>
After completion, create `/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-capability-gate-secure-access-03-SUMMARY.md`
</output>
