---
phase: 01-capability-gate-secure-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .gitignore
  - .env.example
  - docker-compose.yml
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/app/layout.tsx
  - src/lib/env.ts
  - src/lib/db.ts
  - src/lib/password.ts
  - src/lib/ai/server-client.ts
  - src/lib/canva/server-client.ts
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "The app refuses to boot when required server startup secrets are missing or misnamed."
    - "AI and Canva credentials are only available through server-only modules and are not exposed through NEXT_PUBLIC variables."
    - "Internal user accounts exist with hashed passwords so credentials auth and password change can build on real persisted data."
  artifacts:
    - path: ".env.example"
      provides: "Canonical server-only env contract for auth, database, AI, and Canva secrets"
      contains: "AUTH_SECRET"
    - path: "src/lib/env.ts"
      provides: "Validated startup env plus server-only AI/Canva accessors"
      exports: ["env", "validateStartupEnv", "getAiEnv", "getCanvaEnv"]
    - path: "next.config.ts"
      provides: "Actual startup wiring that validates server env before Next boots"
      contains: "validateStartupEnv()"
    - path: "prisma/schema.prisma"
      provides: "User persistence model with passwordHash for credentials auth"
      contains: "model User"
    - path: "prisma/seed.ts"
      provides: "Seeded internal accounts with hashed passwords"
      contains: "passwordHash"
  key_links:
    - from: "next.config.ts"
      to: "src/lib/env.ts"
      via: "startup import and validateStartupEnv() call"
      pattern: "validateStartupEnv\(\)"
    - from: "src/lib/ai/server-client.ts"
      to: "src/lib/env.ts"
      via: "server-only AI credential accessor"
      pattern: "getAiEnv"
    - from: "src/lib/canva/server-client.ts"
      to: "src/lib/env.ts"
      via: "server-only Canva credential accessor"
      pattern: "getCanvaEnv"
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "User create/upsert using passwordHash"
      pattern: "user\.(create|upsert)"
---

<objective>
Create the Phase 1 foundation that safely holds secrets and gives downstream auth and Canva work real contracts to build on.

Purpose: Satisfy SAFE-01 before feature work depends on secrets, and make startup failures obvious instead of leaking or silently misconfiguring credentials.
Output: Bootstrapped Next.js workspace, server-only env contract, startup validation wiring, Prisma user model, password utilities, and seeded internal accounts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/minhnhut_dev/projects/siletravel/.planning/PROJECT.md
@/home/minhnhut_dev/projects/siletravel/.planning/ROADMAP.md
@/home/minhnhut_dev/projects/siletravel/.planning/REQUIREMENTS.md
@/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-CONTEXT.md
@/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-RESEARCH.md

<decisions>
- D-01/D-03/D-04 require username/password auth via NextAuth credentials provider backed by hashed passwords.
- D-05 requires a 7-day session later in auth implementation, so this plan must create the user/password contracts that support that.
- D-09 through D-12 require a repeatable Canva probe later; this plan must create server-only Canva env accessors that 01-03 can import.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Next.js workspace and phase-one tooling contracts</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, docker-compose.yml, src/app/layout.tsx</files>
  <action>Scaffold the Next.js App Router workspace into the existing repository without deleting `.planning/`. Install the Phase 1 runtime/dev dependencies needed by downstream plans: NextAuth.js credentials flow per D-03, Prisma/Postgres persistence, bcrypt password hashing per D-04, Zod env validation, sonner for the D-06 login toast, tsx for scripts, and Playwright for automated auth verification. Add scripts for `typecheck`, `db:up`, `db:migrate`, `db:seed`, `test:e2e`, and `canva:probe`. Keep `src/app/layout.tsx` minimal at this stage; it only needs valid server-rendered HTML and a `lang="vi"` root so downstream auth pages can extend it cleanly. Do not add public marketing pages or any self-registration flow.</action>
  <verify>
    <automated>npm install && npm run lint && npm run typecheck</automated>
  </verify>
  <done>The repository is a runnable Next.js TypeScript app with all Phase 1 dependencies and scripts in place, ready for secure env wiring and auth implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Create the server-only env contract and wire validation into real Next startup per SAFE-01</name>
  <files>.env.example, next.config.ts, src/lib/env.ts, src/lib/ai/server-client.ts, src/lib/canva/server-client.ts</files>
  <action>Create `.env.example` with server-only placeholders for `AUTH_SECRET`, `DATABASE_URL`, `AI_API_URL`, `AI_API_KEY`, `CANVA_CLIENT_ID`, `CANVA_CLIENT_SECRET`, `CANVA_ACCESS_TOKEN`, `CANVA_REFRESH_TOKEN`, and `CANVA_TEMPLATE_ID`. In `src/lib/env.ts`, use Zod plus `import "server-only"` to export: `validateStartupEnv()` for required boot-time vars (`AUTH_SECRET`, `DATABASE_URL`), `env` for shared startup-safe access, `getAiEnv()` for AI-only secrets, and `getCanvaEnv()` for Canva-only secrets. Also reject any `NEXT_PUBLIC_` secret aliases so SAFE-01 is enforceable, not advisory. Update `next.config.ts` to import and call `validateStartupEnv()` during Next startup/server execution so `npm run dev` and `npm run build` fail fast when required server vars are missing. Create `src/lib/ai/server-client.ts` and `src/lib/canva/server-client.ts` as server-only wrappers that read secrets from `getAiEnv()` / `getCanvaEnv()`; these wrappers are the only approved access path for future integrations. Do not expose secrets through client props, client components, API responses, or browser-readable env names.</action>
  <verify>
    <automated>bash -lc 'set -e; AUTH_SECRET=test-secret DATABASE_URL=postgresql://siletravel:siletravel_dev@localhost:5432/siletravel NEXT_TELEMETRY_DISABLED=1 timeout 20s npm run dev >/tmp/siletravel-env-ok.log 2>&1 || { cat /tmp/siletravel-env-ok.log; exit 1; }; if DATABASE_URL=postgresql://siletravel:siletravel_dev@localhost:5432/siletravel NEXT_TELEMETRY_DISABLED=1 timeout 20s npm run dev >/tmp/siletravel-missing-auth.log 2>&1; then cat /tmp/siletravel-missing-auth.log; exit 1; fi; grep -q "AUTH_SECRET" /tmp/siletravel-missing-auth.log; if AUTH_SECRET=test-secret NEXT_TELEMETRY_DISABLED=1 timeout 20s npm run dev >/tmp/siletravel-missing-db.log 2>&1; then cat /tmp/siletravel-missing-db.log; exit 1; fi; grep -q "DATABASE_URL" /tmp/siletravel-missing-db.log; if AUTH_SECRET=test-secret DATABASE_URL=postgresql://siletravel:siletravel_dev@localhost:5432/siletravel NEXT_PUBLIC_CANVA_ACCESS_TOKEN=leak NEXT_TELEMETRY_DISABLED=1 timeout 20s npm run dev >/tmp/siletravel-public-secret.log 2>&1; then cat /tmp/siletravel-public-secret.log; exit 1; fi; grep -Eq "NEXT_PUBLIC_(AI|CANVA)" /tmp/siletravel-public-secret.log'</automated>
  </verify>
  <done>Next startup now validates required server env before serving requests, fails fast when required values are missing, AI/Canva secrets live behind server-only modules, and there is no allowed `NEXT_PUBLIC_*` path for secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Create the user persistence layer, password utilities, and seeded internal accounts</name>
  <files>prisma/schema.prisma, prisma/seed.ts, src/lib/db.ts, src/lib/password.ts, docker-compose.yml</files>
  <action>Create the Postgres local runtime in `docker-compose.yml`, then define a Prisma `User` model with at least `id`, `username`, `name`, `passwordHash`, `mustChangePassword`, `createdAt`, and `updatedAt`. In `src/lib/password.ts`, choose bcryptjs per D-04 and export shared helpers for `hashPassword`, `verifyPassword`, and `assertPasswordPolicy` so the later change-password flow reuses the same rules as seeding. In `prisma/seed.ts`, create assigned internal accounts only per D-02; no self-registration or invite flow. Seed users with hashed passwords and `mustChangePassword: true` so 01-02 can implement the user-accessible password change path on top of real data. Keep `src/lib/db.ts` as a standard Prisma singleton for App Router server usage.</action>
  <verify>
    <automated>docker compose up -d && npx prisma migrate dev --name init_users && npx prisma db seed && npx prisma validate</automated>
  </verify>
  <done>The database contains real internal users with hashed passwords, and downstream auth code has stable `User`, password, and Prisma contracts to import.</done>
</task>

</tasks>

<verification>
- `npm run dev` fails early when `AUTH_SECRET` or `DATABASE_URL` is missing because `next.config.ts` calls `validateStartupEnv()`.
- `.env.example` contains AI and Canva secret names without any `NEXT_PUBLIC_` prefix.
- `src/lib/ai/server-client.ts` and `src/lib/canva/server-client.ts` are server-only wrappers that import from `src/lib/env.ts`.
- `npx prisma db seed` creates assigned internal users with hashed passwords, not plaintext storage.
</verification>

<success_criteria>
- SAFE-01 is concretely wired into real startup/server execution, not just documented.
- The app has a committed env contract for all Phase 1 secrets without committing actual secrets.
- Auth and Canva plans can proceed without redefining env or persistence fundamentals.
</success_criteria>

<output>
After completion, create `/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-capability-gate-secure-access-01-SUMMARY.md`
</output>
