---
phase: 01-capability-gate-secure-access
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - middleware.ts
  - playwright.config.ts
  - tests/e2e/auth.spec.ts
  - src/types/next-auth.d.ts
  - src/lib/auth.config.ts
  - src/lib/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/login/login-toast.tsx
  - src/app/(auth)/login/actions.ts
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/settings/password/page.tsx
  - src/app/(app)/settings/password/actions.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "An assigned internal user can sign in with username/password and reach the protected app."
    - "The session survives browser refresh and remains valid for 7 days from the most recent successful login; each new login resets the 7-day window unless the user logs out or expires."
    - "Every route except /login rejects unauthenticated access by redirecting to /login, showing a toast, and preserving callbackUrl."
    - "A signed-in user can change their own password using the app, and the new password replaces the old hash."
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "NextAuth credentials implementation with 7-day session"
      exports: ["auth", "signIn", "signOut", "handlers"]
    - path: "middleware.ts"
      provides: "App-wide route protection excluding only /login and auth internals"
      contains: "callbackUrl"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Minimal username/password login screen"
      contains: "LoginForm"
    - path: "src/app/(app)/settings/password/page.tsx"
      provides: "User-accessible change password flow"
      contains: "currentPassword"
    - path: "tests/e2e/auth.spec.ts"
      provides: "Automated auth flow verification"
      contains: "redirect"
  key_links:
    - from: "middleware.ts"
      to: "src/app/(auth)/login/page.tsx"
      via: "redirect with callbackUrl and reason query params"
      pattern: "callbackUrl=.*reason=auth-required"
    - from: "src/app/(auth)/login/actions.ts"
      to: "src/lib/auth.ts"
      via: "credentials signIn redirectTo callbackUrl"
      pattern: "signIn\(\"credentials\""
    - from: "src/app/(app)/settings/password/actions.ts"
      to: "src/lib/password.ts"
      via: "verify current password and hash new password"
      pattern: "(verifyPassword|hashPassword)"
    - from: "tests/e2e/auth.spec.ts"
      to: "middleware.ts"
      via: "browser-level redirect/session assertions"
      pattern: "callbackUrl"
---

<objective>
Implement the actual secure access experience for Phase 1 using the previously defined user/env contracts.

Purpose: Fulfill AUTH-01, AUTH-02, and AUTH-03 exactly as the locked decisions describe, including username/password auth, 7-day sessions, redirect-to-login protection, toast feedback, callbackUrl return, and user-accessible password change.
Output: Working credentials auth, protected routing, minimal login UI, protected dashboard shell, change-password flow, and automated end-to-end coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/minhnhut_dev/projects/siletravel/.planning/PROJECT.md
@/home/minhnhut_dev/projects/siletravel/.planning/ROADMAP.md
@/home/minhnhut_dev/projects/siletravel/.planning/REQUIREMENTS.md
@/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-CONTEXT.md
@/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-RESEARCH.md

<interfaces>
Planned contracts from 01-01 that this plan must use directly:

From `src/lib/password.ts`:
```ts
export async function hashPassword(password: string): Promise<string>
export async function verifyPassword(password: string, hash: string): Promise<boolean>
export function assertPasswordPolicy(password: string): void
```

From `src/lib/db.ts`:
```ts
export const db: PrismaClient
```

From `src/lib/env.ts`:
```ts
export const env: {
  AUTH_SECRET: string
  DATABASE_URL: string
}
```
</interfaces>

<decisions>
- D-01: username/password only; no magic links, OAuth, or self-registration.
- D-03: NextAuth.js credentials provider is mandatory.
- D-04: passwords must stay hashed and users must be able to change their own password.
- D-05: session lifetime is 7 days from the most recent successful login, so each new login resets the 7-day window.
- D-06: unauthenticated access redirects to `/login`, shows toast `Vui lòng đăng nhập`, and returns to `callbackUrl` after successful login.
- D-07: login page stays minimal.
- D-08: every route except `/login` is protected.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement NextAuth credentials auth, 7-day sessions, and route protection contracts</name>
  <files>src/types/next-auth.d.ts, src/lib/auth.config.ts, src/lib/auth.ts, src/app/api/auth/[...nextauth]/route.ts, middleware.ts</files>
  <action>Create a NextAuth.js credentials-provider implementation per D-03 using the seeded `User` table from 01-01. `src/lib/auth.ts` must look up users by `username` per D-01, verify the bcrypt hash via the shared password helper per D-04, and expose `auth`, `signIn`, `signOut`, and `handlers`. Configure a JWT session so authentication remains valid for 7 days from the most recent successful login per D-05; each fresh login must mint a new 7-day session window rather than preserving the original one forever, and the implementation notes/tests must treat that most-recent-login reset behavior as part of the contract. Include the user id/username in token/session typing through `src/types/next-auth.d.ts`. In `src/lib/auth.config.ts` and `middleware.ts`, protect all app routes except `/login` and NextAuth internals per D-08. When unauthenticated access hits a protected route, redirect to `/login?callbackUrl=...&reason=auth-required` so the login experience can show the D-06 toast and send the user back to the original destination. Do not introduce public home pages, OAuth providers, email login, or any bypass route beyond `/login` and required auth internals.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>The app has a working credentials-based auth contract with 7-day sessions and middleware-level protection that preserves callback destinations for unauthenticated users.</done>
</task>

<task type="auto">
  <name>Task 2: Build the minimal login UX, toast redirect experience, and protected app shell</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/(auth)/layout.tsx, src/app/(auth)/login/page.tsx, src/app/(auth)/login/login-form.tsx, src/app/(auth)/login/login-toast.tsx, src/app/(auth)/login/actions.ts, src/app/(app)/layout.tsx, src/app/(app)/dashboard/page.tsx</files>
  <action>Implement the minimal `/login` experience from D-07: company logo/title, username field, password field, and submit button only. Mount a global sonner `<Toaster />` in `src/app/layout.tsx`, then add `login-toast.tsx` as a small client component that reads `reason=auth-required` and shows `Vui lòng đăng nhập` per D-06. In the login server action, call `signIn("credentials")` and redirect to the provided `callbackUrl` rather than a hardcoded page. The login page should redirect authenticated users away from `/login` to the requested callback or `/dashboard`. Build a protected app shell with a dashboard placeholder and logout control, and keep `/` as a redirect into the protected app rather than a public landing page. Do not replace the toast with an inline banner-only fallback; the locked decision explicitly requires a toast notification.</action>
  <verify>
    <automated>AUTH_SECRET=test-secret DATABASE_URL=postgresql://siletravel:siletravel_dev@localhost:5432/siletravel npm run build</automated>
  </verify>
  <done>Users see a minimal login page, unauthenticated redirects trigger the required toast, successful sign-in returns to callbackUrl, and the protected shell is only reachable while authenticated.</done>
</task>

<task type="auto">
  <name>Task 3: Add user-accessible password change and automated end-to-end auth verification</name>
  <files>src/app/(app)/settings/password/page.tsx, src/app/(app)/settings/password/actions.ts, playwright.config.ts, tests/e2e/auth.spec.ts</files>
  <action>Create a protected password-change page that asks for `currentPassword`, `newPassword`, and `confirmPassword`. The server action must verify the current hash, enforce the shared password policy from 01-01, hash the new password, update the stored hash, and clear `mustChangePassword` after success per D-04. Then add Playwright coverage for the complete Phase 1 auth slice: unauthenticated visit to `/dashboard` redirects to `/login` with `callbackUrl` and shows the D-06 toast, valid login reaches the protected page, refresh keeps the session alive per AUTH-02, visiting `/login` while authenticated redirects away, logout removes access, wrong password shows an error, and a successful password change allows the new password while rejecting the old one. Include an assertion that signing in again after logout creates a fresh authenticated session window so the D-05 contract remains tied to the most recent successful login. Keep the tests focused on the locked decisions instead of adding unrelated UI coverage.</action>
  <verify>
    <automated>docker compose up -d && npx prisma migrate dev --name auth_flow && npx prisma db seed && npx playwright test tests/e2e/auth.spec.ts --project=chromium</automated>
  </verify>
  <done>The app supports secure self-service password changes and has automated proof that login, redirect protection, session persistence, logout, and callbackUrl behavior all work end to end.</done>
</task>

</tasks>

<verification>
- `tests/e2e/auth.spec.ts` covers the login, redirect, refresh persistence, logout, and password-change flows.
- Unauthenticated access to protected pages results in `/login?callbackUrl=...&reason=auth-required` and a visible toast saying `Vui lòng đăng nhập`.
- The session lasts 7 days from the most recent successful login, and a new login resets that 7-day window.
- Only `/login` remains public; the root route redirects into protected app space.
</verification>

<success_criteria>
- AUTH-01: assigned internal credentials can access the app.
- AUTH-02: refresh preserves the authenticated session.
- AUTH-03: protected routes are inaccessible when logged out and return users to their intended page after login.
- D-04 is implemented concretely through a user-facing change password flow with hashed storage.
</success_criteria>

<output>
After completion, create `/home/minhnhut_dev/projects/siletravel/.planning/phases/01-capability-gate-secure-access/01-capability-gate-secure-access-02-SUMMARY.md`
</output>
