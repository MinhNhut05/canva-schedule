---
phase: 08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/components/ui/sheet.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/app-sidebar.tsx
  - src/app/(app)/history/page.tsx
  - tests/e2e/personal-website-visual-system.spec.ts
autonomous: true
requirements:
  - PW-UI-03
  - PW-UI-05
  - PW-UI-06
must_haves:
  truths:
    - "Các primitive overlay/feedback (sheet, tooltip, alert, alert-dialog) đồng bộ visual system và giữ semantics an toàn."
    - "Navigation + link states có reusable contract rõ ràng (active/hover/focus) tại app-shell thay vì style rời rạc theo từng trang."
    - "Có bề mặt kiểm chứng (proof surface) để downstream phase xác minh token + component contract mà không mở lại tranh luận nền tảng."
    - "Có automated e2e smoke cho font contract, surface split, primitive states, giúp verify nhanh trong vòng 90s."
    - "Không xuất hiện scope leak sang IA/layout strategy/copywriting cá nhân."
  artifacts:
    - path: "src/components/ui/sheet.tsx"
      provides: "Overlay/nav-support primitive theo premium restraint"
      exports: ["Sheet", "SheetTrigger", "SheetContent", "SheetOverlay"]
    - path: "src/components/ui/tooltip.tsx"
      provides: "Micro-hint primitive theo dark premium tone"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
    - path: "src/components/ui/alert.tsx"
      provides: "Shared empty/error/destructive feedback surface"
      exports: ["Alert", "AlertTitle", "AlertDescription"]
    - path: "src/components/ui/alert-dialog.tsx"
      provides: "Destructive confirmation contract dùng lại button variants"
      exports: ["AlertDialog", "AlertDialogContent", "AlertDialogAction", "AlertDialogCancel"]
    - path: "src/components/app-sidebar.tsx"
      provides: "Reusable navigation/link behavior contract cho active/hover/focus states"
      exports: ["AppSidebar"]
    - path: "tests/e2e/personal-website-visual-system.spec.ts"
      provides: "Phase smoke checks cho visual system"
      contains: "surface split, font contract, primitive variants, state effects"
  key_links:
    - from: "src/components/ui/alert-dialog.tsx"
      to: "src/components/ui/button.tsx"
      via: "buttonVariants() cho action/cancel"
      pattern: "buttonVariants\("
    - from: "src/components/app-sidebar.tsx"
      to: "src/components/ui/button.tsx"
      via: "mobile nav trigger dùng Button variant contract"
      pattern: "<Button\s+variant=\"outline\""
    - from: "src/components/app-sidebar.tsx"
      to: "next/link"
      via: "SidebarLink áp active/hover/focus classes theo pathname"
      pattern: "pathname\.startsWith\(item\.href\)|hover:bg-muted|focus-visible:ring-2"
    - from: "tests/e2e/personal-website-visual-system.spec.ts"
      to: "src/app/(app)/history/page.tsx"
      via: "page.goto + locator assertions"
      pattern: "page\.goto\(|expect\("
    - from: "history/page.tsx"
      to: "restyled ui primitives"
      via: "render proof surface qua Button/Card/Badge/Input/Alert/..."
      pattern: "from \"@/components/ui/"
---

<objective>
Hoàn thiện lớp primitive còn lại + navigation/link contract + proof & validation để Phase 8 đủ điều kiện bàn giao cho UI/implementation phase tiếp theo.

Purpose: Chuyển visual decisions thành contract kiểm chứng được, tránh thảo luận lại từ đầu ở phase sau.
Output: Overlay/feedback primitives đã restyle + app-shell navigation/link behavior contract + proof surface + Playwright smoke coverage theo 08-VALIDATION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-CONTEXT.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-UI-SPEC.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-VALIDATION.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle primitives overlay/feedback và khóa navigation-link behavior contract</name>
  <read_first>
    - src/components/ui/sheet.tsx
    - src/components/ui/tooltip.tsx
    - src/components/ui/alert.tsx
    - src/components/ui/alert-dialog.tsx
    - src/components/app-sidebar.tsx
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-CONTEXT.md (D-27, D-31, D-56)
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-UI-SPEC.md (Navigation, Links, Tooltip, Empty/Error/Destructive state)
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-PATTERNS.md (Radix wrapper preservation, consumer-side primitive composition)
  </read_first>
  <files>src/components/ui/sheet.tsx, src/components/ui/tooltip.tsx, src/components/ui/alert.tsx, src/components/ui/alert-dialog.tsx, src/components/app-sidebar.tsx</files>
  <action>
    Retheme overlay/feedback primitives để đồng bộ tokens và effect restraint: sheet overlay/nav tone theo D-27/D-56, tooltip low-noise dark premium, alert default/destructive tách nghĩa rõ, alert-dialog tiếp tục dùng `buttonVariants()` để nhất quán. Song song, cập nhật `src/components/app-sidebar.tsx` để khóa reusable navigation/link contract cho PW-UI-03: active state dựa trên `pathname.startsWith(item.href)`, hover state giữ subtle (`hover:bg-muted` hoặc tương đương semantic), focus-visible rõ (`focus-visible:ring-2 focus-visible:ring-primary`), link style không jump màu mạnh (theo D-31). Giữ nguyên route map, role gating, hành vi sign-out và Radix accessibility wiring; chỉ chỉnh visual/interaction classes và mapping state.
  </action>
  <verify>
    <automated>npm run typecheck && rg -n "pathname\.startsWith\(item\.href\)|hover:bg-muted|focus-visible:ring-2 focus-visible:ring-primary|isActive \?" src/components/app-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Overlay và feedback components cùng ngôn ngữ visual với button/card/input đã có.
    - `src/components/app-sidebar.tsx` chứa contract state có thể grep được: `pathname.startsWith(item.href)` + class cho hover/focus/active.
    - Không phá behavior mở/đóng dialog/sheet, semantics role/aria, hoặc role-based navigation hiện hữu.
  </acceptance_criteria>
  <done>Nhóm primitive bổ trợ + navigation/link behavior hoàn tất, không còn điểm lệch visual lớn trong shared UI layer.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo proof surface tối thiểu để chứng minh contract mà không rơi vào layout strategy</name>
  <read_first>
    - src/app/(app)/history/page.tsx
    - src/components/app-sidebar.tsx
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-UI-SPEC.md
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-VALIDATION.md
  </read_first>
  <files>src/app/(app)/history/page.tsx</files>
  <action>
    Dùng trang history hiện có làm proof surface kỹ thuật (không mở route marketing mới) để render đầy đủ primitive states quan trọng: hero-like dark block + light body block, button variants, card + badge, input/textarea, alert/alert-dialog, tooltip/sheet trigger, và khu vực link/nav-state minh họa theo contract đã khóa ở app-sidebar. Nội dung copy chỉ dùng text hệ thống hoặc nhãn kỹ thuật ngắn, không triển khai IA/copywriting thương hiệu (tuân PW-UI-06, deferred ideas).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Có điểm render xác định cho các nhóm component cần e2e assertion, bao gồm nhóm navigation/link behavior.
    - Proof surface mang tính kỹ thuật và không chứa quyết định layout/narrative website cá nhân.
  </acceptance_criteria>
  <done>Downstream có bề mặt rõ ràng để kiểm chứng contract visual system.</done>
</task>

<task type="auto">
  <name>Task 3: Bổ sung Playwright smoke test cho visual-system contract</name>
  <read_first>
    - tests/e2e/auth.spec.ts
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-VALIDATION.md
    - src/app/(app)/history/page.tsx
    - src/components/app-sidebar.tsx
  </read_first>
  <files>tests/e2e/personal-website-visual-system.spec.ts</files>
  <action>
    Tạo spec mới theo style Playwright hiện có, gồm test cases khớp validation map: `surface split`, `font contract`, `primitive variants`, `state effects`; bổ sung assertion cho navigation/link behavior (active/hover/focus cues) để đóng PW-UI-03 đầy đủ. Assertions tập trung vào class/token hook và trạng thái hiển thị có thể đo được; không dùng snapshot nặng hoặc watch mode. Đảm bảo command chạy đúng trong ≤90s theo 08-VALIDATION.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/personal-website-visual-system.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - Spec chạy pass độc lập và cover đủ 4 nhóm behavior cốt lõi + nav/link behavior contract.
    - Không yêu cầu thao tác manual để xác nhận pass/fail cơ bản.
  </acceptance_criteria>
  <done>Phase có baseline automated validation cho visual contract trước khi vào verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Overlay/feedback primitives → user perception | Sai style có thể làm nhầm trạng thái destructive hoặc mất tín hiệu cảnh báo |
| Navigation/link state cues → user route confidence | Active/hover/focus cues mơ hồ có thể làm user hiểu sai vị trí và khả năng tương tác |
| Proof surface/test selectors → verification reliability | Selector không ổn định dẫn đến false-green, che giấu regression visual contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | S | `alert.tsx` / `alert-dialog.tsx` destructive cues | mitigate | Giữ destructive token riêng biệt, action/cancel phân tách rõ qua buttonVariants |
| T-08-03-02 | I | `sheet.tsx` / `tooltip.tsx` contrast | mitigate | Dùng surface/border tokens đã khóa, tránh overlay quá đậm làm mất readability |
| T-08-03-03 | S | `app-sidebar.tsx` nav/link active-focus states | mitigate | Khóa contract active/hover/focus bằng classes semantic + pattern grep/verifiable trong verify command |
| T-08-03-04 | R | `personal-website-visual-system.spec.ts` | mitigate | Viết assertions deterministic theo behavior map, không phụ thuộc snapshot mơ hồ |
| T-08-03-05 | T | `history/page.tsx` proof surface scope | mitigate | Chỉ render state kỹ thuật; không thêm IA/copy marketing để tránh scope tampering |
</threat_model>

<verification>
- `npm run typecheck`
- `npx playwright test tests/e2e/personal-website-visual-system.spec.ts --project=chromium`
</verification>

<success_criteria>
- Hoàn thành PW-UI-03 cho nhóm navigation/links + overlay/feedback + interaction behaviors.
- Hoàn thành PW-UI-05 bằng proof surface + automated smoke có thể tái chạy.
- Hoàn thành PW-UI-06 bằng ràng buộc scope rõ ràng, không kéo sang layout/content strategy.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-03-SUMMARY.md`
</output>
