---
phase: 08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - PW-UI-01
  - PW-UI-02
  - PW-UI-04
must_haves:
  truths:
    - "Người dùng nhìn thấy nền tảng visual thống nhất dark hero + light body với accent xanh điện được khóa theo quyết định pha 8."
    - "Typography toàn cục đã khóa Space Grotesk cho heading và Inter cho body/UI ở root layout, không còn font drift theo component."
    - "Token bề mặt/viền/shadow/glow/motion ở mức tiết chế, dùng lại được cho primitive mà không phải hardcode per-page."
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Wiring font biến CSS toàn cục (Space Grotesk + Inter)"
      exports: ["RootLayout", "metadata"]
    - path: "src/app/globals.css"
      provides: "Semantic token + @theme inline bridge + utility nền tảng visual"
      contains: ":root, @theme inline, token màu/typography/radius/shadow/motion"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "className và CSS variable trên <body>"
      pattern: "variable: '--font-.*'"
    - from: "src/app/globals.css"
      to: "các component ui dùng bg-*/text-*/ring-*"
      via: "semantic token mapping"
      pattern: "--color-(background|foreground|primary|ring)"
---

<objective>
Khóa nền visual system ở tầng root để mọi component phía sau chỉ cần bám token và font contract đã định.

Purpose: Loại bỏ drift màu/font giữa các khu vực UI, đảm bảo Phase 8 đi đúng design-system-first.
Output: `layout.tsx` có font wiring chuẩn + `globals.css` có token contract đồng bộ với quyết định D-01..D-13, D-21..D-26, D-47..D-54.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-CONTEXT.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-UI-SPEC.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-RESEARCH.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring font toàn cục bằng next/font và khóa typography root</name>
  <read_first>
    - src/app/layout.tsx
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-CONTEXT.md (D-07, D-08, D-09)
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-UI-SPEC.md (Typography contract)
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-PATTERNS.md (Root shell wiring)
  </read_first>
  <files>src/app/layout.tsx</files>
  <action>
    Thêm `Space_Grotesk` và `Inter` qua `next/font/google`, expose CSS variables ở root, áp vào `body` để khóa hệ font theo D-07. Giữ nguyên shell hiện tại (`metadata`, `Toaster`, `html lang="vi"`) và không đụng copy/layout nội dung trang (tuân D-08, PW-UI-06). Giữ đúng pattern analog trong 08-PATTERNS: mở rộng root shell wiring hiện có thay vì thay đổi cấu trúc `RootLayout`. Nếu metadata đang mang nội dung sản phẩm cũ, KHÔNG chỉnh trong phase này.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` có cả 2 font variables cho Space Grotesk + Inter và compile sạch.
    - Không thay đổi cấu trúc layout/IA hay text marketing ngoài phạm vi font wiring.
  </acceptance_criteria>
  <done>Font contract toàn cục ổn định, sẵn sàng cho token/component styling.</done>
</task>

<task type="auto">
  <name>Task 2: Thiết lập semantic token và utility nền visual dark-hero/light-body</name>
  <read_first>
    - src/app/globals.css
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-CONTEXT.md (D-01..D-06, D-10..D-13, D-21..D-26, D-47..D-54)
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-UI-SPEC.md (Color, Surface, Token mapping)
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-PATTERNS.md (Semantic token bridge)
  </read_first>
  <files>src/app/globals.css</files>
  <action>
    Cập nhật `:root` + `@theme inline` để phản ánh chính xác palette/tokens đã khóa: dark hero + light body (D-01, D-02, D-03), bảng màu lõi (D-04, D-05, D-06), radius/shadow/border/motion seed (D-22..D-24, D-50..D-54). Bổ sung utility nền tảng (ví dụ hero surface/light surface/glow/border semantic) chỉ ở mức design-system primitives; không thêm layout section hay copywriting. Bắt buộc giữ pattern hai lớp token bridge như 08-PATTERNS (`:root` semantic vars + `@theme inline` mapping), không chuyển sang hardcode màu trực tiếp trong component-level CSS.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` giữ contract 2 lớp `:root` + `@theme inline`, token đồng bộ, không đứt utility mapping.
    - Có token/utility đủ để primitive tiêu thụ mà không cần hardcode hex ở consumer.
    - Readability guardrails vẫn giữ: accent không dùng cho body dài, hiệu ứng mạnh tập trung hero.
  </acceptance_criteria>
  <done>Nền token đã khóa và reusable cho toàn bộ plan sau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token layer → interactive primitives | Sai token có thể làm mờ trạng thái focus/disabled và gây hiểu nhầm trạng thái UI |
| Root font config → toàn bộ app shell | Font wiring sai gây fallback không kiểm soát, phá hierarchy và readability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | S | `src/app/globals.css` focus/ring token | mitigate | Giữ `--ring`/focus contract rõ, kiểm bằng compile + e2e plan sau |
| T-08-01-02 | I | Body text on surface tokens | mitigate | Khóa dark-text-on-light và white-text-on-dark theo D-34..D-37, không đặt body text trên glow mạnh |
| T-08-01-03 | T | Root font variables | mitigate | Dùng `next/font/google` + CSS variable root để tránh override rời rạc từng component |
</threat_model>

<verification>
- `npm run typecheck`
- Kiểm tra diff đảm bảo chỉ chạm `layout.tsx`, `globals.css`
</verification>

<success_criteria>
- Hoàn thành PW-UI-01 (nền visual split), PW-UI-02 (font contract), PW-UI-04 (token/effect guardrails tầng gốc).
- Mọi plan sau có thể consume token/font mà không cần hỏi lại quyết định nền tảng.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-01-SUMMARY.md`
</output>
