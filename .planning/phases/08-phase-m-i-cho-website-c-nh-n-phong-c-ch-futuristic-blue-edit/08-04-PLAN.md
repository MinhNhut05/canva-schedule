---
phase: 08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - src/app/(app)/history/page.tsx
  - tests/e2e/personal-website-visual-system.spec.ts
  - src/components/ui/card.tsx
  - src/components/ui/alert.tsx
autonomous: true
gap_closure: true
requirements:
  - PW-UI-03
  - PW-UI-05
must_haves:
  truths:
    - "Trang proof surface `/history` chỉ lộ dữ liệu cho current user, trừ khi session role là admin thì mới xem toàn bộ lịch sử."
    - "Query param `page` không làm route `/history` vỡ khi nhận giá trị rác, âm, không hữu hạn, hoặc thiếu."
    - "Smoke suite của Phase 8 có thể rerun ổn định như một proof gate thay vì phụ thuộc vào path login dễ fail ngẫu nhiên."
    - "Shared primitive typings khớp với DOM heading thực tế nên ref contract của proof surface và downstream UI không lệch kiểu."
  artifacts:
    - path: "src/app/(app)/history/page.tsx"
      provides: "Scoped history proof surface với pagination parse an toàn và query guard theo user/role"
      exports: ["default"]
    - path: "tests/e2e/personal-website-visual-system.spec.ts"
      provides: "Rerunnable smoke validation cho visual proof gate"
      contains: "ổn định login/setup path hoặc session bootstrap cho /history assertions"
    - path: "src/components/ui/card.tsx"
      provides: "CardTitle ref type khớp với h3"
      exports: ["CardTitle"]
    - path: "src/components/ui/alert.tsx"
      provides: "AlertTitle ref type khớp với h5"
      exports: ["AlertTitle"]
  key_links:
    - from: "src/app/(app)/history/page.tsx"
      to: "auth session"
      via: "session.user.id + session.user.role tạo Prisma where"
      pattern: "session\.user\.(id|role)|where"
    - from: "src/app/(app)/history/page.tsx"
      to: "prisma.upload.findMany / prisma.upload.count"
      via: "shared where filter + skip/take an toàn"
      pattern: "prisma\.upload\.(findMany|count)"
    - from: "tests/e2e/personal-website-visual-system.spec.ts"
      to: "/history"
      via: "stable auth/setup rồi page.goto('/history')"
      pattern: "page\.goto\(\"/history\"\)|beforeEach"
    - from: "src/components/ui/card.tsx"
      to: "history proof surface headings"
      via: "CardTitle renders h3 with matching heading ref type"
      pattern: "React\.forwardRef<\s*HTMLHeadingElement"
    - from: "src/components/ui/alert.tsx"
      to: "history proof surface alerts"
      via: "AlertTitle renders h5 with matching heading ref type"
      pattern: "React\.forwardRef<\s*HTMLHeadingElement"
---

<objective>
Đóng các gap còn lại mà verifier/reviewer đã nêu để Phase 8 có proof surface an toàn, rerunnable, và đủ tin cậy làm validation gate cho downstream UI work.

Purpose: Hoàn tất PW-UI-05 bằng cách vá authorization gap, defensive pagination parsing, Playwright smoke stability, và type-contract mismatch nhẹ trong shared primitives mà proof surface đang dùng.
Output: Một `/history` proof route an toàn hơn, smoke spec rerunnable, và shared heading primitives có ref typing đúng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-CONTEXT.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-RESEARCH.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-PATTERNS.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-VERIFICATION.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-REVIEW.md
@.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-03-SUMMARY.md

<interfaces>
From src/app/(app)/history/page.tsx:
```tsx
interface HistoryPageProps {
  searchParams: Promise<{ page?: string }>;
}

export default async function HistoryPage({ searchParams }: HistoryPageProps)
```

From src/components/ui/card.tsx:
```tsx
export { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle };
```

From src/components/ui/alert.tsx:
```tsx
export { Alert, AlertDescription, AlertTitle };
```

From tests/e2e/personal-website-visual-system.spec.ts:
```ts
test.describe("Personal website visual system", () => {
  test.beforeEach(async ({ page }) => {
    await page.context().clearCookies();
    await page.goto("/login");
    // login bootstrap
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope `/history` data to current user/admin and harden pagination parsing</name>
  <read_first>
    - src/app/(app)/history/page.tsx
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-VERIFICATION.md
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-REVIEW.md
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-03-SUMMARY.md
  </read_first>
  <files>src/app/(app)/history/page.tsx</files>
  <action>Update the `/history` server page so the proof surface remains on the existing route but no longer leaks team-wide upload data. Build a single Prisma `where` object from the authenticated session: admin can read all rows; non-admin users are limited to `userId = session.user.id`, per the verifier gap and consistent with the Phase 5 role model already recorded in STATE.md. Replace `Number(params.page ?? 1)` with defensive parsing that accepts only positive finite integers and falls back to page 1 for invalid input (`abc`, `1x`, `0`, negative, empty, Infinity, NaN). Keep the existing proof surface, pagination UI, and Phase 8 visual/content boundaries unchanged; do not expand route scope into new layout/content strategy work.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `findMany` and `count` share the same `where` filter derived from `session.user.role` and `session.user.id`.
    - Invalid `page` values degrade to `1` without producing `NaN` skip values or Prisma validation failure.
    - The technical proof surface on `/history` still renders after the data-safety fix.
  </acceptance_criteria>
  <done>`/history` is safe enough to serve as Phase 8 proof surface and no longer fails on malformed `page` query strings.</done>
</task>

<task type="auto">
  <name>Task 2: Make Phase 8 smoke validation rerunnable and repair heading ref type mismatches</name>
  <read_first>
    - tests/e2e/personal-website-visual-system.spec.ts
    - tests/e2e/auth.spec.ts
    - src/components/ui/card.tsx
    - src/components/ui/alert.tsx
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-VERIFICATION.md
    - .planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-REVIEW.md
  </read_first>
  <files>tests/e2e/personal-website-visual-system.spec.ts, src/components/ui/card.tsx, src/components/ui/alert.tsx</files>
  <action>Stabilize the visual-system smoke spec so it can be rerun as a dependable gate instead of timing out in `beforeEach` on the login/navigation bootstrap. Reuse the project’s existing Playwright auth style from `tests/e2e/auth.spec.ts`, but make the setup deterministic for this spec: clear cookies, establish a valid authenticated session in one stable path, then navigate to `/history` only after auth is known-good. Keep assertions focused on the existing proof surface contract from Plan 08-03; do not broaden the spec into layout/content strategy checks. In the same task, correct the shared primitive type mismatches flagged by review: `CardTitle` must use `HTMLHeadingElement` for the rendered `<h3>`, and `AlertTitle` must use `HTMLHeadingElement` for the rendered `<h5>`. Do not change public exports, DOM element choices, or visual class contracts while fixing types.</action>
  <verify>
    <automated>npx playwright test tests/e2e/personal-website-visual-system.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/personal-website-visual-system.spec.ts` passes on a rerun without depending on a flaky `page.goto("/login")` bootstrap path.
    - The spec still validates the existing Phase 8 proof behaviors rather than introducing new website-scope assertions.
    - `CardTitle` and `AlertTitle` use ref typings that match their heading DOM nodes.
  </acceptance_criteria>
  <done>PW-UI-05 regains a stable automated smoke gate, and the proof surface’s shared heading primitives no longer carry ref type drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → `/history` data query | Session identity crosses into Prisma filters; incorrect scoping leaks other users’ upload metadata |
| URL query string → pagination math | Untrusted `page` input can corrupt `skip`/`take` if parsing is not defensive |
| Playwright setup path → proof gate reliability | Flaky auth bootstrap can create false negatives and make verification non-rerunnable |
| shared primitive types → downstream ref consumers | Wrong ref element types weaken integration safety for reused heading primitives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | I | `src/app/(app)/history/page.tsx` | mitigate | Scope `findMany`/`count` with a shared `where` derived from `session.user.role` and `session.user.id`; admin-only full visibility |
| T-08-04-02 | D | `src/app/(app)/history/page.tsx` pagination parse | mitigate | Parse `page` with finite positive integer guard and fallback to `1` before computing `skip` |
| T-08-04-03 | R | `tests/e2e/personal-website-visual-system.spec.ts` | mitigate | Replace brittle login bootstrap with deterministic authenticated setup so reruns prove the same gate every time |
| T-08-04-04 | T | `src/components/ui/card.tsx` / `src/components/ui/alert.tsx` | mitigate | Align `forwardRef` generics with actual heading elements while preserving exports and DOM structure |
</threat_model>

<verification>
- `npm run typecheck`
- `npx playwright test tests/e2e/personal-website-visual-system.spec.ts --project=chromium`
- Confirm diff only touches the four gap-closure files listed in this plan.
</verification>

<success_criteria>
- Phase 8 verifier gaps for `/history` authorization scope, `page` parsing safety, and Playwright rerunnability are closed.
- Shared primitive heading refs are type-correct with no scope drift into layout/content strategy.
- PW-UI traceability mismatch in `REQUIREMENTS.md` is explicitly left deferred unless a separate planning cleanup is approved, because it is a planning-source normalization issue rather than a blocking implementation gap for this proof gate.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-m-i-cho-website-c-nh-n-phong-c-ch-futuristic-blue-edit/08-04-SUMMARY.md`
</output>
