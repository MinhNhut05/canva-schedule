---
plan: 02-04
phase: 2
title: "Text Extractors and Normalization"
requirements:
  - DOC-04
depends_on:
  - 02-01
files_modified:
  - src/lib/documents/extract-pdf.ts
  - src/lib/documents/extract-docx.ts
  - src/lib/documents/normalize.ts
autonomous: true
must_haves:
  truths:
    - "PDF extraction produces readable Vietnamese text with preserved diacritics via pdfjs-dist"
    - "DOCX extraction produces readable Vietnamese text via mammoth.extractRawText()"
    - "Text normalization applies NFC Unicode normalization and preserves Vietnamese diacritics exactly"
    - "Normalization never ASCII-folds or strips combining marks"
    - "PDF extractor tracks textItemCount for scanned-PDF quality detection"
    - "DOCX extractor tracks lineCount for fragmentation quality detection"
  artifacts:
    - file: "src/lib/documents/extract-pdf.ts"
      purpose: "PDF text extraction adapter using pdfjs-dist legacy build"
    - file: "src/lib/documents/extract-docx.ts"
      purpose: "DOCX text extraction adapter using mammoth.extractRawText()"
    - file: "src/lib/documents/normalize.ts"
      purpose: "Unicode normalization, whitespace cleanup, Vietnamese diacritic preservation"
  key_links:
    - from: "pipeline.ts (Plan 02-05)"
      to: "extract-pdf.ts / extract-docx.ts"
      via: "direct function import based on DocumentKind"
    - from: "pipeline.ts (Plan 02-05)"
      to: "normalize.ts"
      via: "normalizeText() called on raw extraction output"
---

# Plan 02-04: Text Extractors and Normalization

## Goal

Build the PDF and DOCX text extraction adapters and the text normalization module. These are the core parsing components that produce readable Vietnamese text from uploaded files. The pipeline orchestrator (Plan 02-05) will call these functions.

---

## Wave 2 — Extraction Adapters

<task id="02-04-01" wave="2" autonomous="true">
  <title>Build PDF text extraction adapter using pdfjs-dist</title>
  <requirements>DOC-04</requirements>
  <files>
    - src/lib/documents/extract-pdf.ts (create)
    - src/lib/documents/types.ts (read — DocumentKind)
    - package.json (read — confirm pdfjs-dist dependency)
    - .planning/phases/02-document-intake-parsing/02-RESEARCH.md (read — pdfjs-dist import path, per-page extraction, Pitfall 3 and 6)
  </files>
  <action>
    Create `src/lib/documents/extract-pdf.ts`:

    ```typescript
    import "server-only";

    interface PdfExtractionOutput {
      rawText: string;
      pageCount: number;
      warnings: string[];
      textItemCount: number;
    }

    export async function extractPdfText(
      data: Uint8Array
    ): Promise<PdfExtractionOutput> {
      const pdfjsLib = await import("pdfjs-dist/legacy/build/pdf.mjs");
      const { getDocument } = pdfjsLib;

      const pdf = await getDocument({ data }).promise;
      const pages: string[] = [];
      const warnings: string[] = [];
      let totalTextItems = 0;

      for (let pageNumber = 1; pageNumber <= pdf.numPages; pageNumber += 1) {
        const page = await pdf.getPage(pageNumber);
        const content = await page.getTextContent();
        const strings = content.items
          .map((item: any) => ("str" in item ? item.str : ""))
          .filter(Boolean);
        totalTextItems += strings.length;
        pages.push(strings.join(" "));
        page.cleanup();
      }

      if (totalTextItems === 0 && pdf.numPages > 0) {
        warnings.push(
          "PDF có trang nhưng không có nội dung text. File có thể là ảnh scan."
        );
      }

      return {
        rawText: pages.join("\n\n"),
        pageCount: pdf.numPages,
        warnings,
        textItemCount: totalTextItems,
      };
    }
    ```

    Key details:
    - Dynamic import `pdfjs-dist/legacy/build/pdf.mjs` for Node.js compatibility
    - Per-page `getTextContent()` extraction
    - Track `textItemCount` for quality scoring
    - Vietnamese warning for scanned PDFs with diacritics
    - `page.cleanup()` for resource management
  </action>
  <verify>
    <automated>
      test -f src/lib/documents/extract-pdf.ts && echo "PASS: extract-pdf.ts exists" || echo "FAIL"
      grep -q "extractPdfText" src/lib/documents/extract-pdf.ts && echo "PASS: function exported" || echo "FAIL"
      grep -q 'import("pdfjs-dist/legacy/build/pdf.mjs")' src/lib/documents/extract-pdf.ts && echo "PASS: dynamic import" || echo "FAIL"
      grep -q "getTextContent" src/lib/documents/extract-pdf.ts && echo "PASS: text extraction" || echo "FAIL"
      grep -q "page.cleanup" src/lib/documents/extract-pdf.ts && echo "PASS: cleanup" || echo "FAIL"
      grep -q "textItemCount" src/lib/documents/extract-pdf.ts && echo "PASS: text item tracking" || echo "FAIL"
      grep -q "không có nội dung text" src/lib/documents/extract-pdf.ts && echo "PASS: Vietnamese warning with diacritics" || echo "FAIL"
      grep -q 'import "server-only"' src/lib/documents/extract-pdf.ts && echo "PASS: server-only" || echo "FAIL"
      npx tsc --noEmit 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    - extract-pdf.ts exports extractPdfText
    - Uses dynamic import for pdfjs-dist legacy build
    - Tracks textItemCount and pageCount
    - Vietnamese warning uses full diacritics
    - `npx tsc --noEmit` passes
  </done>
</task>

<task id="02-04-02" wave="2" autonomous="true">
  <title>Build DOCX text extraction adapter using mammoth</title>
  <requirements>DOC-04</requirements>
  <files>
    - src/lib/documents/extract-docx.ts (create)
    - src/lib/documents/types.ts (read — DocumentKind)
    - package.json (read — confirm mammoth dependency)
    - .planning/phases/02-document-intake-parsing/02-RESEARCH.md (read — mammoth extractRawText, Pitfall 4 and 5)
  </files>
  <action>
    Create `src/lib/documents/extract-docx.ts`:

    ```typescript
    import "server-only";
    import mammoth from "mammoth";

    interface DocxExtractionOutput {
      rawText: string;
      warnings: string[];
      lineCount: number;
    }

    export async function extractDocxText(
      buffer: Buffer
    ): Promise<DocxExtractionOutput> {
      const result = await mammoth.extractRawText({ buffer });
      const warnings = result.messages.map((message) => message.message);
      const lines = result.value.split("\n").filter((line) => line.trim().length > 0);

      return {
        rawText: result.value,
        warnings,
        lineCount: lines.length,
      };
    }
    ```

    Key details:
    - Uses `mammoth.extractRawText()` — NOT `convertToHtml()` (Pitfall 5)
    - Counts non-empty lines for fragmentation detection
    - mammoth@1.12.0 ships built-in types
  </action>
  <verify>
    <automated>
      test -f src/lib/documents/extract-docx.ts && echo "PASS: extract-docx.ts exists" || echo "FAIL"
      grep -q "extractDocxText" src/lib/documents/extract-docx.ts && echo "PASS: function exported" || echo "FAIL"
      grep -q "mammoth.extractRawText" src/lib/documents/extract-docx.ts && echo "PASS: extractRawText used" || echo "FAIL"
      grep -qv "convertToHtml" src/lib/documents/extract-docx.ts && echo "PASS: no convertToHtml" || echo "FAIL"
      grep -q "lineCount" src/lib/documents/extract-docx.ts && echo "PASS: line count tracking" || echo "FAIL"
      grep -q 'import "server-only"' src/lib/documents/extract-docx.ts && echo "PASS: server-only" || echo "FAIL"
      npx tsc --noEmit 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    - extract-docx.ts exports extractDocxText
    - Uses mammoth.extractRawText() only
    - Tracks lineCount for quality scoring
    - `npx tsc --noEmit` passes
  </done>
</task>

<task id="02-04-03" wave="2" autonomous="true">
  <title>Build text normalization module</title>
  <requirements>DOC-04</requirements>
  <files>
    - src/lib/documents/normalize.ts (create)
    - src/lib/documents/types.ts (read — ExtractionResult normalizedText field)
    - .planning/phases/02-document-intake-parsing/02-RESEARCH.md (read — Pattern 5: normalization steps)
  </files>
  <action>
    Create `src/lib/documents/normalize.ts`:

    ```typescript
    /**
     * Normalize extracted text for quality scoring and downstream use.
     * Steps:
     * 1. Unicode NFC normalization
     * 2. Convert CRLF/CR to LF
     * 3. Collapse repeated spaces (preserve paragraph breaks)
     * 4. Trim lines and overall
     * 5. NEVER ASCII-fold — preserve Vietnamese diacritics exactly
     */
    export function normalizeText(raw: string): string {
      let text = raw;
      text = text.normalize("NFC");
      text = text.replace(/\r\n/g, "\n");
      text = text.replace(/\r/g, "\n");
      text = text.replace(/[^\S\n]+/g, " ");
      text = text.replace(/\n{3,}/g, "\n\n");
      text = text.split("\n").map((line) => line.trim()).join("\n");
      text = text.trim();
      return text;
    }
    ```

    Pure logic module, no server-only needed, zero external dependencies.
  </action>
  <verify>
    <automated>
      test -f src/lib/documents/normalize.ts && echo "PASS: normalize.ts exists" || echo "FAIL"
      grep -q "export function normalizeText" src/lib/documents/normalize.ts && echo "PASS: function exported" || echo "FAIL"
      grep -q '.normalize("NFC")' src/lib/documents/normalize.ts && echo "PASS: NFC normalization" || echo "FAIL"
      grep -q '\\r\\n' src/lib/documents/normalize.ts && echo "PASS: CRLF handling" || echo "FAIL"
      grep -qv 'normalize("NFD")' src/lib/documents/normalize.ts && echo "PASS: no NFD stripping" || echo "FAIL"
      npx tsc --noEmit 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    - normalize.ts exports normalizeText
    - Applies NFC normalization
    - Handles CRLF conversion
    - No ASCII-folding or diacritic stripping
    - `npx tsc --noEmit` passes
  </done>
</task>

---

## Verification

After all tasks complete:
1. `npx tsc --noEmit` — passes
2. All 3 modules exist: extract-pdf.ts, extract-docx.ts, normalize.ts
3. No `convertToHtml` usage anywhere
4. No diacritic stripping patterns
