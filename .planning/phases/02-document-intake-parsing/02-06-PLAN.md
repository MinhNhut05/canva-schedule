---
plan: 02-06
phase: 2
title: "Extraction Result UI and Core Unit Tests"
requirements:
  - DOC-03
  - DOC-04
  - DOC-05
depends_on:
  - 02-03
  - 02-05
files_modified:
  - src/app/(app)/upload/extraction-result.tsx
  - src/app/(app)/upload/upload-form.tsx
  - src/lib/documents/intake.test.ts
  - src/lib/documents/extractors.test.ts
autonomous: true
must_haves:
  truths:
    - "Raw text is displayed in a scrollable reading panel with preserved line breaks (whitespace-pre-wrap)"
    - "Advisory yellow warning banner appears for warning and poor quality levels with Vietnamese copy"
    - "Quality badges display Vietnamese text: 'Tốt', 'Cần kiểm tra', 'Thấp'"
    - "User can always proceed regardless of quality (D-09: advisory, not blocking)"
    - "Unit tests pass for intake validation and extractors"
  artifacts:
    - file: "src/app/(app)/upload/extraction-result.tsx"
      purpose: "Extraction result display with quality badges and warning banner"
    - file: "src/lib/documents/intake.test.ts"
      purpose: "Unit tests for file validation module"
    - file: "src/lib/documents/extractors.test.ts"
      purpose: "Unit tests for PDF/DOCX extraction and normalization"
  key_links:
    - from: "upload-form.tsx result state"
      to: "extraction-result.tsx"
      via: "React props (data + onReset callback)"
    - from: "extraction-result.tsx onReset"
      to: "upload-form.tsx state reset"
      via: "callback prop clearing selectedFile, result, error"
---

# Plan 02-06: Extraction Result UI and Core Unit Tests

## Goal

Build the extraction result display component with quality warning banners, then write unit tests for intake validation and text extractors. All visible text uses Vietnamese with diacritics (D-12).

---

## Wave 4 — Result UI + Core Unit Tests

<task id="02-06-01" wave="4" autonomous="true">
  <title>Build extraction result display component with quality warning banner</title>
  <requirements>DOC-04, DOC-05</requirements>
  <files>
    - src/app/(app)/upload/extraction-result.tsx (create)
    - src/app/(app)/upload/upload-form.tsx (modify — integrate extraction-result)
    - src/lib/documents/types.ts (read — ExtractionResult, QualityLevel)
    - src/components/ui/card.tsx (read — Card component)
    - src/components/ui/alert.tsx (read — Alert component)
    - src/components/ui/badge.tsx (read — Badge component)
    - src/lib/utils.ts (read — cn utility)
    - .planning/phases/02-document-intake-parsing/02-UI-SPEC.md (read — Sections 5-6: Vietnamese copy, warning banner, quality badges)
  </files>
  <action>
    1. Create `src/app/(app)/upload/extraction-result.tsx` as client component:

       **Result summary header:**
       - Heading: `Kết quả trích xuất`
       - Metadata: `Tên file`, `Loại file`, `Thời gian xử lý`, `Chất lượng`
       - Quality badges (Vietnamese with diacritics):
         - good → green Badge: `Tốt`
         - warning → yellow Badge: `Cần kiểm tra`
         - poor → orange Badge: `Thấp`

       **Quality warning banner (conditional, only for warning/poor):**
       - Background: `bg-[#FEF3C7]`, text: `text-[#92400E]`
       - Poor title: `Chất lượng trích xuất thấp`
       - Poor body: `Nội dung lấy ra có thể không chính xác. Bạn có thể tiếp tục kiểm tra hoặc tải file khác để thử lại.`
       - Warning title: `Nội dung cần kiểm tra lại`
       - Warning body: `Hệ thống đã trích xuất được văn bản nhưng có dấu hiệu chất lượng chưa ổn. Hãy xem kỹ trước khi dùng cho bước tiếp theo.`
       - Actions: `Tiếp tục kiểm tra`, `Tải file khác`

       **Raw text preview:**
       - `whitespace-pre-wrap`, scrollable, `max-h-[500px]`
       - Display `normalizedText` as plain text (NEVER dangerouslySetInnerHTML)
       - Empty placeholder: `Văn bản trích xuất sẽ hiển thị ở đây sau khi xử lý.`

       **Next-step note:**
       - `Bạn có thể kiểm tra nhanh nội dung bên dưới trước khi chuyển sang bước trích xuất AI ở Phase 3.`

    2. Update `upload-form.tsx` to render `<ExtractionResult>` when result is set:
       - Pass `onReset` callback that clears result, selectedFile, error
       - Show status badge: `Đã trích xuất` (good) or `Đã trích xuất kèm cảnh báo` (warning/poor)
  </action>
  <verify>
    <automated>
      test -f src/app/\(app\)/upload/extraction-result.tsx && echo "PASS: extraction-result.tsx exists" || echo "FAIL"
      grep -q '"use client"' src/app/\(app\)/upload/extraction-result.tsx && echo "PASS: client component" || echo "FAIL"
      grep -q "whitespace-pre-wrap" src/app/\(app\)/upload/extraction-result.tsx && echo "PASS: text display mode" || echo "FAIL"
      grep -qv "dangerouslySetInnerHTML" src/app/\(app\)/upload/extraction-result.tsx && echo "PASS: no HTML injection" || echo "FAIL"
      grep -q "#FEF3C7" src/app/\(app\)/upload/extraction-result.tsx && echo "PASS: warning background color" || echo "FAIL"
      grep -q "ExtractionResult\|extraction-result" src/app/\(app\)/upload/upload-form.tsx && echo "PASS: result component integrated" || echo "FAIL"
      grep -q "onReset" src/app/\(app\)/upload/upload-form.tsx && echo "PASS: onReset callback" || echo "FAIL"
      npx tsc --noEmit 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    - extraction-result.tsx displays quality badges in Vietnamese with diacritics
    - Warning banner uses correct colors and Vietnamese copy
    - Raw text displayed with whitespace-pre-wrap, no HTML injection
    - upload-form.tsx integrates result component with onReset
    - `npx tsc --noEmit` passes
  </done>
</task>

<task id="02-06-02" wave="4" autonomous="true">
  <title>Write file validation unit tests</title>
  <requirements>DOC-03</requirements>
  <files>
    - src/lib/documents/intake.test.ts (create)
    - src/lib/documents/intake.ts (read — validateFile, error messages)
    - src/lib/documents/types.ts (read — FileValidationResult, MAX_FILE_SIZE_BYTES)
    - vitest.config.ts (read — test config)
    - tests/fixtures/documents/sample-tour-vi.pdf (read — valid PDF fixture)
    - tests/fixtures/documents/sample-tour-vi.docx (read — valid DOCX fixture)
    - tests/fixtures/documents/not-a-pdf.bin (read — invalid fixture)
  </files>
  <action>
    Create `src/lib/documents/intake.test.ts`:

    Mock `server-only`: `vi.mock("server-only", () => ({}));`

    Test cases (at least 7):
    1. Returns valid: false with Vietnamese error when file is null
    2. Returns valid: false with Vietnamese error when file exceeds 30MB
    3. Returns valid: false for .txt file
    4. Returns valid: false for .jpg file with JPEG magic bytes
    5. Returns valid: false for .pdf with non-PDF binary content
    6. Returns valid: true with kind "pdf" for real PDF fixture
    7. Returns valid: true with kind "docx" for real DOCX fixture

    All error message assertions must check for Vietnamese with diacritics:
    - Check for "Vui lòng chọn" (with diacritics, NOT "Vui long chon")
    - Check for "không được hỗ trợ" (with diacritics, NOT "khong duoc ho tro")
    - Check for "vượt quá" (with diacritics, NOT "vuot qua")

    Run: `npx vitest run src/lib/documents/intake.test.ts`
  </action>
  <verify>
    <automated>
      test -f src/lib/documents/intake.test.ts && echo "PASS: test file exists" || echo "FAIL"
      grep -q 'vi.mock("server-only"' src/lib/documents/intake.test.ts && echo "PASS: server-only mock" || echo "FAIL"
      grep -c "it(" src/lib/documents/intake.test.ts | xargs -I {} test {} -ge 5 && echo "PASS: >= 5 test cases" || echo "FAIL"
      npx vitest run src/lib/documents/intake.test.ts 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - intake.test.ts has 7+ test cases
    - Tests verify Vietnamese diacritics in error messages
    - Tests cover null file, oversized, wrong extension, wrong binary, valid PDF, valid DOCX
    - `npx vitest run src/lib/documents/intake.test.ts` passes
  </done>
</task>

<task id="02-06-03" wave="4" autonomous="true">
  <title>Write extraction adapter and normalization unit tests</title>
  <requirements>DOC-04</requirements>
  <files>
    - src/lib/documents/extractors.test.ts (create)
    - src/lib/documents/extract-pdf.ts (read — extractPdfText)
    - src/lib/documents/extract-docx.ts (read — extractDocxText)
    - src/lib/documents/normalize.ts (read — normalizeText)
    - vitest.config.ts (read — test config)
    - tests/fixtures/documents/sample-tour-vi.pdf (read — valid PDF fixture)
    - tests/fixtures/documents/sample-tour-vi.docx (read — valid DOCX fixture)
    - tests/fixtures/documents/empty-no-text.pdf (read — empty PDF fixture)
  </files>
  <action>
    Create `src/lib/documents/extractors.test.ts`:

    Mock `server-only`: `vi.mock("server-only", () => ({}));`

    Test suites:
    1. `describe("extractPdfText")`:
       - Extracts Vietnamese text from valid PDF with diacritics
       - Returns zero text items for scanned/empty PDF with Vietnamese warning

    2. `describe("extractDocxText")`:
       - Extracts Vietnamese text from valid DOCX

    3. `describe("normalizeText")`:
       - Normalizes Unicode to NFC
       - Converts CRLF to LF
       - Collapses spaces, preserves paragraph breaks
       - Preserves Vietnamese diacritics exactly (test with "Chuong trinh tour du lich Da Nang")

    Run: `npx vitest run src/lib/documents/extractors.test.ts`
  </action>
  <verify>
    <automated>
      test -f src/lib/documents/extractors.test.ts && echo "PASS: test file exists" || echo "FAIL"
      grep -q 'vi.mock("server-only"' src/lib/documents/extractors.test.ts && echo "PASS: server-only mock" || echo "FAIL"
      grep -q 'describe("extractPdfText")' src/lib/documents/extractors.test.ts && echo "PASS: PDF tests" || echo "FAIL"
      grep -q 'describe("extractDocxText")' src/lib/documents/extractors.test.ts && echo "PASS: DOCX tests" || echo "FAIL"
      grep -q 'describe("normalizeText")' src/lib/documents/extractors.test.ts && echo "PASS: normalize tests" || echo "FAIL"
      npx vitest run src/lib/documents/extractors.test.ts 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - 3 test suites: extractPdfText, extractDocxText, normalizeText
    - PDF test verifies Vietnamese diacritics in output
    - Empty PDF test verifies warnings and zero textItemCount
    - Normalize test verifies diacritics preserved exactly
    - `npx vitest run src/lib/documents/extractors.test.ts` passes
  </done>
</task>

---

## Verification

After all tasks complete:
1. `npx tsc --noEmit` — passes
2. `npx vitest run src/lib/documents/intake.test.ts` — validation tests pass
3. `npx vitest run src/lib/documents/extractors.test.ts` — extraction tests pass
4. extraction-result.tsx renders quality badges and warning banners in Vietnamese
5. upload-form.tsx integrates result component with reset capability
