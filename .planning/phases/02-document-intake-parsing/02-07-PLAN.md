---
plan: 02-07
phase: 2
title: "Quality Scoring Tests, E2E Tests, and Phase Gate"
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
depends_on:
  - 02-06
files_modified:
  - src/lib/documents/quality.test.ts
  - tests/e2e/document-intake.spec.ts
autonomous: true
must_haves:
  truths:
    - "All quality scoring unit tests pass covering all 6 flags"
    - "E2E tests cover upload flow, error handling, and extraction result display"
    - "All visible UI text assertions use Vietnamese with full diacritics"
    - "Phase 2 complete gate: tsc, vitest, and e2e all pass"
  artifacts:
    - file: "src/lib/documents/quality.test.ts"
      purpose: "Unit tests for quality scoring signals including extractor metadata"
    - file: "tests/e2e/document-intake.spec.ts"
      purpose: "End-to-end tests for complete upload and extraction flow"
  key_links:
    - from: "quality.test.ts"
      to: "quality.ts scoreQuality(QualityInput)"
      via: "direct import — tests pass kind, pageCount, textItemCount, lineCount explicitly"
    - from: "e2e document-intake.spec.ts"
      to: "upload page → extraction-result.tsx"
      via: "Playwright browser automation"
---

# Plan 02-07: Quality Scoring Tests, E2E Tests, and Phase Gate

## Goal

Write quality scoring unit tests that exercise the explicit extractor metadata contract, then write end-to-end tests for the complete Phase 2 upload-to-extraction flow. Conclude with the Phase 2 complete gate.

---

## Wave 5 — Quality Tests + E2E + Phase Gate

<task id="02-07-01" wave="5" autonomous="true">
  <title>Write quality scoring unit tests with extractor metadata</title>
  <requirements>DOC-05</requirements>
  <files>
    - src/lib/documents/quality.test.ts (create)
    - src/lib/documents/quality.ts (read — scoreQuality, QualityInput, thresholds)
    - src/lib/documents/types.ts (read — QualityResult, QualityFlag)
    - vitest.config.ts (read — test config)
  </files>
  <action>
    Create `src/lib/documents/quality.test.ts`:

    Test cases (at least 8):
    1. Returns level "good" for well-formed Vietnamese text (500+ chars with diacritics)
    2. Flags textTooShort for very short text
    3. Flags containsReplacementChars for garbled text with U+FFFD
    4. Flags lowVietnameseSignal for ASCII-only text
    5. Flags likelyScannedPdf when kind='pdf', pageCount=5, textItemCount=0
    6. Flags fragmentedDocx when kind='docx', lineCount with low avg chars/line
    7. Does NOT flag likelyScannedPdf when kind='docx' (even if pageCount/textItemCount provided)
    8. Does NOT flag fragmentedDocx when kind='pdf' (even if lineCount provided)

    **Important:** All test calls to `scoreQuality()` must pass `QualityInput` with explicit `kind` field and relevant metadata fields, validating the data contract from Plan 02-05.

    Run: `npx vitest run src/lib/documents/quality.test.ts`
  </action>
  <verify>
    <automated>
      test -f src/lib/documents/quality.test.ts && echo "PASS: test file exists" || echo "FAIL"
      grep -q 'import { scoreQuality }' src/lib/documents/quality.test.ts && echo "PASS: scoreQuality import" || echo "FAIL"
      grep -q 'kind:' src/lib/documents/quality.test.ts && echo "PASS: kind field in test calls" || echo "FAIL"
      grep -q 'pageCount:' src/lib/documents/quality.test.ts && echo "PASS: pageCount in test calls" || echo "FAIL"
      grep -q 'textItemCount:' src/lib/documents/quality.test.ts && echo "PASS: textItemCount in test calls" || echo "FAIL"
      grep -q 'lineCount:' src/lib/documents/quality.test.ts && echo "PASS: lineCount in test calls" || echo "FAIL"
      grep -c "it(" src/lib/documents/quality.test.ts | xargs -I {} test {} -ge 6 && echo "PASS: >= 6 test cases" || echo "FAIL"
      npx vitest run src/lib/documents/quality.test.ts 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - 8+ test cases covering all 6 quality flags
    - All scoreQuality() calls pass QualityInput with explicit kind, pageCount, textItemCount, lineCount
    - Tests verify good/warning/poor level classification
    - Tests verify format-specific flags don't cross-apply (pdf flags skip for docx, vice versa)
    - `npx vitest run src/lib/documents/quality.test.ts` passes
  </done>
</task>

<task id="02-07-02" wave="5" autonomous="true">
  <title>Write e2e tests for complete upload and extraction flow</title>
  <requirements>DOC-01, DOC-02, DOC-03, DOC-04, DOC-05</requirements>
  <files>
    - tests/e2e/document-intake.spec.ts (create)
    - tests/e2e/auth.spec.ts (read — existing login pattern)
    - playwright.config.ts (read — test config)
    - src/app/(app)/upload/page.tsx (read — page structure)
    - src/app/(app)/upload/upload-form.tsx (read — form elements)
    - src/app/(app)/upload/extraction-result.tsx (read — result display)
    - tests/fixtures/documents/sample-tour-vi.pdf (read — fixture)
    - tests/fixtures/documents/sample-tour-vi.docx (read — fixture)
    - tests/fixtures/documents/empty-no-text.pdf (read — fixture)
    - .planning/phases/02-document-intake-parsing/02-UI-SPEC.md (read — Vietnamese copy)
  </files>
  <action>
    Create `tests/e2e/document-intake.spec.ts`:

    beforeEach: login with test credentials

    Active tests (at least 6):
    1. Upload page accessible, shows Vietnamese title with diacritics
    2. Upload PDF shows file preview with metadata
    3. Upload DOCX shows file preview with metadata
    4. Rejects unsupported file with Vietnamese error (diacritics)
    5. Sidebar shows Vietnamese nav labels
    6. Clicking submit button submits file, shows processing state, then result
    7. Shows extraction result after processing
    8. Shows quality warning for empty/scanned PDF
    9. Reset action resets upload form

    All text assertions use Vietnamese with diacritics.
  </action>
  <verify>
    <automated>
      test -f tests/e2e/document-intake.spec.ts && echo "PASS: e2e test file exists" || echo "FAIL"
      grep -q 'import { test, expect }' tests/e2e/document-intake.spec.ts && echo "PASS: Playwright imports" || echo "FAIL"
      grep -q "test.beforeEach\|beforeEach" tests/e2e/document-intake.spec.ts && echo "PASS: login setup" || echo "FAIL"
      grep -c 'test(' tests/e2e/document-intake.spec.ts | xargs -I {} test {} -ge 6 && echo "PASS: >= 6 active tests" || echo "FAIL"
      grep -q "setInputFiles" tests/e2e/document-intake.spec.ts && echo "PASS: file upload via setInputFiles" || echo "FAIL"
      grep -q "sample-tour-vi.pdf" tests/e2e/document-intake.spec.ts && echo "PASS: PDF fixture reference" || echo "FAIL"
      grep -q "empty-no-text.pdf" tests/e2e/document-intake.spec.ts && echo "PASS: empty PDF fixture" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - 6+ active e2e tests covering upload, validation, extraction, and warning
    - All text assertions use Vietnamese with full diacritics
    - Sidebar assertions verify Vietnamese labels
    - Fixture files referenced correctly
    - No test.skip for extraction result or warning tests
  </done>
</task>

---

## Phase 2 Complete Gate

After all tasks in 02-07 complete, verify the entire Phase 2:

1. `npx tsc --noEmit` — passes
2. `npx vitest run` — all unit tests pass (intake, extractors, quality)
3. `npx vitest run src/lib/documents/intake.test.ts` — validation tests pass
4. `npx vitest run src/lib/documents/extractors.test.ts` — extraction tests pass
5. `npx vitest run src/lib/documents/quality.test.ts` — quality tests pass (with explicit QualityInput)
6. Navigate to `/upload` → upload PDF → see Vietnamese text in raw text panel
7. Upload empty PDF → see yellow warning banner with Vietnamese quality warning
8. Warning is advisory only — raw text panel still accessible
9. All visible UI text is Vietnamese with diacritics (no ASCII-only Vietnamese)
